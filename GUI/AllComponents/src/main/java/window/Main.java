package window;

public class Main {
    
    public static void main(String[] args) {
        
        // creates new object of Window class
        Window window1 = new Window();
        
        /*
        * all the windows created from the JFrame Class are no-visible,
        * so we have to set it visible, calling the method below
        */
        window1.setVisible(true);
        
    }
}
